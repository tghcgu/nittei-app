import SiteLayout from '../SiteLayout'
export { metadata } from '../SiteLayout'

export default function JapaneseLayout({ children }: { children: React.ReactNode }) {
  return <SiteLayout>{children}</SiteLayout>
}
