---
title: "入力の手間を減らす日程調整サービス「日程組」を作った話"
emoji: "📅"
type: "idea"
topics: ["nextjs", "typescript", "supabase", "cloudflare", "個人開発"]
published: false
---

# 入力の手間を減らす日程調整サービス「日程組」を作った話

日程調整サービス「日程組」を作りました。

https://nittei-app.qoj.workers.dev/

候補日を作ってURLを共有するだけで、参加者がログイン不要で回答できる日程調整・出欠管理ツールです。

この記事では、実装の細かい話よりも「なぜ作ったか」「どこを便利にしたかったか」「作ってみて考えたこと」を中心に書きます。

## 作ったもの

主催者がイベント名と候補日を入力すると、共有URLが発行されます。

参加者はURLを開いて、各候補日に対して次の4つで回答できます。

- `○`
- `△`
- `✕`
- `-`

記号の意味はサービス側で固定せず、イベント作成者が説明欄にルールを書けるようにしました。

たとえば「`○`: 絶対OK」「`△`: 他卓調整中」「`✕`: NG」「`-`: その他」みたいに、そのイベントに合わせたルールを書けます。

さらに `-` を選んだ日には、その日ごとの補足コメントを残せます。「夕方以降ならOK」「仕事次第」のような、1つの記号だけでは伝わりにくい状況を共有するためです。

## なぜ作ったか

日程調整はシンプルな作業に見えて、意外と面倒です。

特に面倒だと感じたのは次の2つでした。

- 候補日をたくさん入力すること
- 回答する側が自分の予定を見ながら1つずつ入力すること

候補日が数日なら手入力でも問題ありません。

でも、1か月分の候補日を出したい場合や、同じ日に複数の時間帯がある場合は、入力だけでかなり手間になります。

回答する側も同じで、カレンダーを見ながら1つずつ `○ / △ / ✕` を押すのは地味に大変です。

そこで「候補日を作る側」と「回答する側」の両方で、できるだけ入力の手間を減らすことを重視しました。

## 候補日入力を楽にする

日程組では、候補日を1日ずつ追加するだけでなく、まとめて追加できるようにしています。

主な入力方法は次の通りです。

- 1日ずつ追加
- 日付範囲でまとめて追加
- カレンダーから複数日を選択
- 曜日ごとに一括選択・解除
- この月の今日以降をまとめて選択
- `.ics / .zip` から予定がある日を除外

カレンダーUIでは、日付を長押ししてなぞるように選択できます。

スマホでも使うことを考えると、ボタンを1つずつ押すより、指でそのまま選べる方が自然だと思いました。

## Googleカレンダーのzipをそのまま読めるようにした

カレンダー連携で一番便利にしたかったのが、`.ics / .zip` の読み込みです。

Googleカレンダーから予定を書き出すと、`.ics` ファイルではなくzipでダウンロードされることがあります。

そのため、日程組ではzipをそのままアップロードできるようにしました。

処理の流れはざっくり次のような感じです。

```text
Googleカレンダーからzipをエクスポート
  ↓
日程組にそのままアップロード
  ↓
zip内の .ics を読み込む
  ↓
予定と候補日が重なるか判定
  ↓
予定がある日を候補日から外す / 回答に反映する
```

ファイルはブラウザ内で処理し、サーバーには送信・保存しません。

また、Googleカレンダーのエクスポートzipには誕生日カレンダーが含まれることがあるので、誕生日カレンダーは自動で除外するようにしました。

## 回答する側もカレンダーを読み込める

主催者だけでなく、回答者側でも `.ics / .zip` を読み込めます。

予定が候補日と重なる場合、複数の候補日へまとめて記号を入力できます。

デフォルトでは、

- 予定あり: `✕`
- 予定なし: `○`

として反映されます。

さらに、予定あり・予定なしのそれぞれで、入力する記号を選べるようにしました。

たとえば「予定がある日は `-` にしてコメントしたい」「空いている日だけ `○` にしたい」みたいな使い方もできます。

## スマホでなぞって回答できるようにした

候補日が多い場合、回答ボタンを1つずつ押すのも面倒です。

そこで、回答ボタンを長押しして、そのまま上下になぞると複数行にまとめて同じ回答を入力できるようにしました。

横にずらすと別の記号も入力できます。

たとえば `○` を長押しして下になぞれば、複数日をまとめて `○` にできます。

この機能は便利なのですが、スマホのスクロール操作とぶつかりやすいところが難しかったです。

最初からスクロールを止めてしまうと、普通にページを動かしたいときまで邪魔になります。

そこで、次のようにしました。

- タップだけなら1つだけ入力
- 長押しが成立するまではスクロールを優先
- 長押し後は回答入力を優先
- 指が画面端に近づいたら自動スクロール

このあたりは、実際にスマホで触りながら何度も調整しました。

## 戻る / 進むはかなり大事だった

候補日をまとめて追加したり、回答を一括変更したりできるようにすると、操作ミスの影響も大きくなります。

そこで、候補日編集にも回答入力にも「戻る / 進む」を入れました。

まとめて操作できる機能は便利ですが、間違えたときに戻せないと怖くて使いにくいです。

一括操作と取り消しはセットで考えた方がいいと感じました。

## 技術スタック

使っている技術は以下です。

| 役割 | 技術 |
| --- | --- |
| フレームワーク | Next.js 16 App Router |
| UI | React 19 |
| 言語 | TypeScript |
| スタイリング | Tailwind CSS 4 |
| データベース | Supabase |
| ホスティング | Cloudflare Workers |
| Next.jsアダプター | OpenNext for Cloudflare |
| カレンダー解析 | ical.js |
| zip展開 | fflate |
| 並び替え | dnd-kit |
| 計測 | Cloudflare Web Analytics |

実装寄りに見ると、特に大きかったのは `.ics` 解析とスマホ操作まわりです。

`.ics` は終日予定、時間つき予定、繰り返し予定などがあるので、単純な日付リストとしては扱えません。

また、スマホの長押し入力は、スクロールと競合しないようにする必要がありました。

## ログイン不要にした理由

日程調整では、参加者にアカウント作成を求めると回答のハードルが上がると思っています。

「URLを開く」

「名前を入れる」

「回答する」

くらいの軽さにしたかったので、ログイン不要にしました。

一方で、ログイン不要にすると編集権限の設計には工夫が必要になります。

今は使いやすさを優先し、SupabaseのRLSもログインなしの操作を許可する公開ポリシーにしています。RLSを有効にしただけで利用者ごとの所有権が生まれるわけではないため、今後は編集用トークンを分けるなど、より安心して使える仕組みも検討したいです。

また、使い終わったデータを無期限に残さないため、最終更新から365日を過ぎたイベントはCloudflare Cron Triggerで自動削除するようにしました。

## 作ってみて感じたこと

個人開発では、機能を増やすこと自体よりも「その機能を安心して使えるか」が大事だと感じました。

たとえば、候補日を一括追加できるだけだと、間違えたときに怖いです。

でも、戻る / 進むがあれば使いやすくなります。

スマホでなぞって回答できるだけでも、スクロールしづらいと使いにくいです。

だから、便利な機能を作るときは、その機能で起きる失敗やストレスも一緒に潰していく必要があると思いました。

## 今後やりたいこと

- 編集権限まわりの強化
- `.ics / .zip` のファイルサイズ制限
- Googleカレンダー連携
- OGP画像の改善
- 回答一覧の見やすさ改善
- 大量候補日・大量回答時のパフォーマンス改善

## まとめ

ログイン不要の日程調整サービス「日程組」を作りました。

特にこだわったのは、候補日入力と回答入力の手間を減らすことです。

範囲指定、カレンダー選択、`.ics / .zip` 読み込み、長押し入力、戻る / 進むなどを入れて、できるだけ少ない操作で日程調整できるようにしました。

よかったら触ってみてください。

https://nittei-app.qoj.workers.dev/
